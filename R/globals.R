globalVariables(unique(c(
  # mod_exploration_raw_data_server : <anonymous>:
  "DATE", "mean_RESULTAT", "PARAMETRE_GROUPE", "PARAMETRE_LIBELLE", "PROGRAMME", "RESULTAT", "TAXON_LIBELLE", "THEME", "YEAR"
)))
